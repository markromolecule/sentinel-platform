# Issue

- when I try to use oauth on [sentinel-mobile], after the google oauth instead of navigating back to the [sentinel-mobile] it redirects me to the production which is the [sentinel-web].

I am using react-native in expo go. Can you double check why this happening and resolve this issue. After oauth of the google instead on sentinel web it should navigate me on the expo go back and authenticates to classroom page

Check the .env and what urls I should add on the supabase

- I want [sentinel-mobile] is just for mobile no need to bridge to [sentinel-web] since we are just using the same supabase and uses [sentinel-api] for the backend
