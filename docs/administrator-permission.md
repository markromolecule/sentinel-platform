# Goal

- Implement a dynamic way to handle the permission on administrator

1. Sections
2. Subjects
3. Subject List
4. Subject Classification
5. Enrollment Requests
6. Offered Subjects
7. Courses

This is for the dynamic for the UI components such as the [Buttons] and the [Action Button] per table and pages

For e.g:

- If the support sets the permission of the [Sections] to [View] only. The [Create], [Update], [Delete] buttons should not be visible to the [Administrator]
- If the support sets the permission of the [Subjects] to [View] only. The [Create], [Update], [Delete] buttons should not be visible to the [Administrator]

Deeply analyze all of it including the backend modules in that way you have a clear context for all the functions that usable in the frontend for the [Administrator]
