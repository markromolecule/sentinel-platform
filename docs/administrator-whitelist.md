# Goal

- Ensure the student whitelist on the administrator page shows only the student whitelist from the specific branch only.
- Ensure to create a column for the origin institution name on the student whitelist table. Instead of showing the [claimed_by] which is currently there, change it to show the origin column. But ensure that don't remove that column, but instead we can just hide it on the [toggle column] feature.
