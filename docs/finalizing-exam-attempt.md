# Issues

1. The [RIGHT_CLICK_ATTEMPT] is not working correctly, and it does not trigger the right-click incident including the alert.
2. The [PRINT_SCREEN_ATTEMPT] is not working correctly, and it does not trigger the print screen incident including the alert.
    - Include the [cmd + shift + 3] for mac
    - [windows + shift + s] for windows
    - [printscreen] button for windows

## Implement a feature

1. Implement an export button exam-card as [pdf].
2. It will be structured based on the following
    - question type
    - section of the questions
    - includes a header of the exam
    - includes also a field for the student name, student id, and date
