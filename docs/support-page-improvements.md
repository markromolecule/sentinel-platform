# Goal

- remove the superadmins and support button on the administrator management. header on support page
- ui issue we need to remove or have a better naming for the settings on the telemetry page for e.g

1. 'enable' telemetry ingestion - it raise a concern whether the title is to enable or what the settings, so, we need to remove the word 'enable' to or suggest a better naming for the settings
2. 'enable' batching

focus on the telemetry page for those words with 'enable'

telemetry -> rule overrides: can you make the web rules and mobile rules as columns in that way they are in 1 container but separated by columns this is to minimize the scroling on the page
