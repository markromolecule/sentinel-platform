# Goal

- Refactor the UI / UX of the [telemetry] page @app/sentinel-support/src/app/(protected)/(support)/telemetry

1. Make sure to follow the structure / layout of the [control] page @app/sentinel-support/src/app/(protected)/(support)/control

- Page must also use a left sidebar similar to the control page
- Page must also use a top bar similar to the control page
- Page must also have a main content area similar to the control page

2. Make sure to use the same components and how it was built

- Ensure that it will have the same components, structure, layout of the control page
