# Goal

- Add a bulk-import on the student whitelist in support page
- Instead of dropdown for the student whitelist, use the facets
- Ensure that all the buttons including the bulk-import, add whitelist entry, action button including the delete and reassign entry on the whitelist from sentinel-core should also have on the whitelist in the sentinel support
- Create a new path for creating support account, currently the sentinel-support only has the superadmin creation which is the dean management. Ensure to include that new page on the support because we recently delete the header that handles the superadmin and support account creation.
