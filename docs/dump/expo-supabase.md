# Goal

To connect now the [sentinel-mobile] to [supabase]

- For now, we settle the [authentication] pages on the expo
- Connect the [sentinel-mobile] to [sentinel-api] in that way we can use the the backend:

@app/sentinel-api/src/modules/identity/auth
@app/sentinel-api/src/modules/identity/onboarding
@app/sentinel-api/src/modules/identity/users

I am using [Expo Go] - React Native
