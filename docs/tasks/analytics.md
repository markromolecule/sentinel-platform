# UI Improvement

- Improve the analytics page on administrator.

1. Ensure to expand the analytics all the data we can show on the [analytics] page
2. Ensure the component are minimalist and compact and use a shadcn components / or the reusabl components on shared/ui
    - You can add new component at @packages/ui/src/components/ui/ if needed.

3. Ensure to expand the scope of the [Analytics] this is to ensure a better analytics for the system.
4. Ensure to create a [mock-data] for now. It should be align on what the database currently have; you can check the prisma
    - @packages/shared/src/mock-data ; this is to visualize the improve on the [analytics] page and ensure that all data are now align to the schema and types that we have this is to preparation for the connection to backend [sentinel-api] for later after the [ui] improvements
