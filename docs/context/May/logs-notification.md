# Goal

- We want to fully implemented all the [activities], [logs], [notifications] that we can GET all of these [DATA] for our [Notification], [Logs], [Analytics] modules.

# How it should work

- Notification should fetch all/entire [DATA], [ACTIVITIES], [TRANSACTIONS], [LOGS] made by the [USER], under the [INSTITUTION] and [PARENT_INSTITUTION] in these way. The system does not just [CREATED], [UPDATE], [DELETER] but it notifies across the system for all the [TRANSACTIONS].

- The [Analytics] and [Logs] need a full-scope for the important part of the [System] - You can decice to expand it to have a better analytics and logs of the system. This will ensure that all the data are credible and made from the system.

For reference. Fully analyze from end-to-end the
@docs/capstone/end-to-end-activity-matrix.md as it shows the full context of each modules from the backend including the [activities], [transactions], etc
