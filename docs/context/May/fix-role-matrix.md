# Fix Role Pages

1. Remove the sub-item on the sentinel-support [access-control]
2. Fix the path to [role-matrix] it returns 404
3. Improve the ui for the dialogs on the 
    - new role
    - new permissions
    - new assignments

Currently it uses square button unlike the others and you should ensure to provide a better layout and ux for the users