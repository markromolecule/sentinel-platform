# Goal

- To connect and create the frontend of the announcement page acorss the system
- Use the mutation and query from the shared to avoid creating new on each system
    @packages/hooks/src/query/announcements
    @packages/services/src/api/announcements.ts
- Ensure to use the resuable components from @packages/ui/src/components/ui
- You need to improve the current announcement page for better UX for the user