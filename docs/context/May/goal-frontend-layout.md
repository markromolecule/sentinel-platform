# Goal

- To redesign the layout of the pages and have a similar shell to the [access-control], [subjects], and [telemetry] pages. Which uses a layout with sidebar that will display the [sub-items]. In that way we can eliminate those on the [sidebar] of the @app/sentinel-core/src/components/sidebar

1. The sidebar that holds the [organization] which is the entry is the department
   @app/sentinel-core/src/app/(protected)/departments
   @app/sentinel-core/src/app/(protected)/semesters
   @app/sentinel-core/src/app/(protected)/rooms

2. System logs
   @app/sentinel-core/src/app/(protected)/logs

3. Analytics
   @app/sentinel-core/src/app/(protected)/analytics
