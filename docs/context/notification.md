# Goal

- Currently, the logs are being implemented. But, we need also to expand the [Notification].

Check all the [controllers], [data], [services] that handles the [activities] & [transactions]. Ensure that it will throw a [Notification] because currently, upon code review I only see [Logs]

Reference:
@docs/capstone/end-to-end-activity-matrix.md

- Ensure that all the [Activities] & [Transactions] usually CRUDs, you decide what should be the [notification].
- Keep the [Logs] as well. But the most important is to implement the [Notification] also under their respected [Controllers], [Data], [Services].

You need to deeply analyze the @docs/capstone/end-to-end-activity-matrix.md as it holds all the context you need for this implementation
