# Goal

- Change the layout of the identity & access in sentinel-core into the new layout similar to the layout on the telemetry page and access-control page. You can check also the layout on the subject page in the sentinel-core.
- Ensure to cleanup the sidebar of the administartor to remove the sub-item and let the new layout handle the items 