# Goal

- To redesign the layout of the subject page on the 

1. Support [sentinel-support]
2. Administrator [sentinel-core] 
3. Instructor [sentinel-web]

Follow the workshell for the [access-control] and [telemetry] page on the [sentinel-support]. Whereas, it uses a sidebar layout. We will convert the [sub-items] of the [subject] page into a page sidebar similarly on the [access-control] and [telemetry].

It should contain the path to
1. subject list
2. subject classifications
3. offered subjects
4. enrollment requests

Check this files for reference on how it structure the page
@app/sentinel-support/src/app/(protected)/(support)/telemetry
@app/sentinel-support/src/app/(protected)/(support)/telemetry/_components/layout