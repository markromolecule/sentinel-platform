# Goal

- To improve the ux of the lobby and the monitoring page on the instructor
    - Re-design the layout of the lobby and the monitoring page. You should apply the sidebar layout from the other pages like the subject page which uses sidebar layout
    - You should put on the sidebar which is the [Lobby] and the [Monitoring]
    - Improve the UX on the [Lobby] page, you should think of a better layout and better ui without overexaggerating the components and keeping it still simple and modern, also, use debounce for the components
