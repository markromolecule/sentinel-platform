# Goal

- Fetch all the [offered_subjects] under the [institution] and to make the administrator can now create a [classroom] and assign it to a [instructor] using [search] for the user / instructor to make it more easier to find the instructor to be assigned on a specific classroom. In this way, even if the instructor is not on the [department] they can still be assigned

- Add a [bulk] import on the [section] management page on the [sentinel-core] this is similar on the [sentinel-support] section management in which handles [bulk] import on sections