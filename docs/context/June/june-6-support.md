# Goal

- To create a page for support
    1. Messages
    2. Announcement

- Ensure that the UI for those pages are similar on the sentinel-core
- Ensure to u pdate the sidebar for the new path pages

# Issue

- I notice that, the sidebar consume a couple of seconds before rendering the management items on the sidebar. What fix can you do about it? This refers on the pages that holds a data, it took a quite seconds before rendering
- The [avatar] on the sentinel-support for the user-search-bar is not showing the correct profile picture. But on the sentinel-core it does work, double check this issue