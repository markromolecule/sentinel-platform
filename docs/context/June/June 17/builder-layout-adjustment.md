# Goal

- To redesign the layout of the [builder] page.
    - Follow the layout of the [subject] page which is a sidebar layout and uses workshell.
        - Instead of [sidebar] that contains [sub-item] it should contain the [exam-rules] and [configuration], this sidebar should showcase those settings with better UX.
- To provide a better UX for the current container of the [section] on the builder page.
    - Right now, the [section] and the [instruction] showcase a poor UI in which results to a bad user experience. Think of a better approach on where the user can include the [instruction] for the [section] of the questions.
