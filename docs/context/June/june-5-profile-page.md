# Goal

- to create now the profile page /or account preferences
- to make the header dynamic for sentinel-core, sentinel-support, sentinel-web - it should now fetch the name of the user, email, profile when the icon for user clicked
- feature added: a search bar so the user can directly search a [user] and then will be redirected to [messages] in that way there's another way to search a [user]. - ensure to use shadacn components and ensure to make it reusable in that way we dont need to repeat. it
- update the sentinel-support - wherein, it only prints "support portal", make it now dyanmic to fetch also the [institution] of the [user] thats currently logged on