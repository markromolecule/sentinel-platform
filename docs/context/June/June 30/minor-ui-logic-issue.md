# Issue

# sentinel-core

- the [view] of the [offered-subject-list] is showing [assigned-instructor] that is not [assigned] and has a existing [classroom].
    - it shows [previous] [assigned-instructor]
    - ensure that it will show only the [available] [classrooms] and is currently [assigned-instructor]

# sentinel-support

- the [course] page - [add course] button is not clickable for the support role even if the permission is enabled for the support role
- the [subject classification] page below pagination ui is not good fix it
- the [enrollment request] page
    - [Table] Column with id 'institution' does not exist.

# sentinel-web, sentinel-core, sentinel-support

- the dashabord anoouncement side is showing only [1] announcement
    - improve ui by implementing it shows 3 then apply a fade for the 4th announcement

# sentinel-web

- replicate the ui of the calendar page of the [sentinel-core] in this way its mirror or uniform to other

# sentinel-core, sentinel-support

- ensure that the calendar page [add event] dialog is functioning well
    - specifically for the [target audience]
        - ensure that when its assigned to [role] its [limited] to that role only
    - improve the ux for the date, start time and end time whereas can we streamline this part of the dialog, since it raise a concern since it was inconvenient to the user
    - ensure to create a [test] files for this changes
