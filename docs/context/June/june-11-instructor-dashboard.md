# Goal

- To replicate the dashboard of sentinel-core to sentinel-web: instructor
    - Ensure that the components are the same
    - Ensure that the functionalities are the same
    - Ensure that the sizing, margins, padding, colors, and other styling are the same
    - Ensure that the responsiveness is the same
    - Ensure that the features are the same
