# Goal

1. Course Management: sentinel-support
    - Add a button to create course on [course page]
    - Add a checkbox at the start on the [table header], that will allow user to select multiple courses and perform actions on them specifically [delete]

2. Subject Management: sentinel-support
    - Add a checkbox on page at the start the table header, that will allow user to select multiple subjects and perform actions on them specifically [delete]
        1. Subject List table
        2. Offered Subject table

3. Section Management: sentinel-support
    - Add a checbox on the [section list] page at the start on the table header, that will allow user to select multiple sections and perform actions on them specifically [delete]

4. Identity & Access page: sentinel-support
    - Add a checbox on the [user list] page at the start on the table header, that will allow user to select multiple users and perform actions on them specifically [delete]
        1. Overview page
        2. Dean management
        3. Support maangement
