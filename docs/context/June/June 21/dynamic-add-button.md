# Goal

- To make the 'add', 'setup wizard', 'bulk upload' 'create' button dynamic on [sentinel-support] based on the permission it has.
    - Ensure that if the user permission is not there, the button should not be visible.
    - Ensure to check also for the action columns for 'edit' 'delete' 'view' and so on. These buttons should also be dynamic based on the permission it has.
    - Make sure it is implemented in a generic way so that it can be used in other pages as well.

- This same goes applies to [sentinel-core] pages. 
    - Investigate the codebase for any 'add', 'setup wizard', 'bulk upload' 'create' button, and make it dynamic based on the permission it has.
    - Ensure to check also for the action columns for 'edit' 'delete' 'view' and so on. These buttons should also be dynamic based on the permission it has.
    - Make sure it is implemented in a generic way so that it can be used in other pages as well.