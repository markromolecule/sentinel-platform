# Goal

- redesign the [search] on the header and make it as search bar and position it at the center of the header
- update the [sentinel-support] header from statis "support portal" to dynamic fetch of what the user institution similarly on the [sentinel-core]
- ensure that the dropdown menu on the header will fetches the full name of the user, currently it just return / prints "user"
- ensure that the icon for the header on the user will now. fetch the [image] / [profile_picture] of the user usually if the user logs in using gmail account check the supabase for this this applies to all headers on the system sentinel-core, sentinel-support, sentinel-web