# Goal

- Refactor the support dashboard as in from scratch

Required needs for the dashboard

On the right side - add wrapper that serves as sidebar for the page this is similar on the [subject] page that appears on the left side but in dashboard for support, it should appear on the right side that will contain:

1. Calendar area - 
2. Announcement area

* use shadcn components for calendar

on the main page, it should show the overview cards such as the departments, etc datas that needed to be show and still implements the draggable of the cards from right to left like a carousel

add a text saying "Good morning / this depends on the time of the day", "full name" and the current date in the top left corner of the main page under that will show a sub text you can decide what to put

for now this is what I thought and strictly follow this first and if you think we need to change some things let's talk about it
