# Fix 002 Execution Log: Student Experience Contracts

**Plan:** [fix-002-implementation-plan-student-experience-contracts.md](./fix-002-implementation-plan-student-experience-contracts.md)

## Phase 0: Baseline, Diagnostics, and Stop Conditions

- [x] Read the implementation plan and confirmed the execution rules require phase-by-phase completion with a pause between phases.
- [x] Scanned the current exam visibility, telemetry ingestion, incident persistence, audio worker, and recovery surfaces referenced by the plan.
- [x] Confirmed the workspace provides the expected API, web, and database package scripts, including `sentinel-api:start:telemetry-worker` and workspace test runners.
- [x] Confirmed `.env.example` exists for `app/sentinel-api` and that environment-variable alignment is part of the deployment contract.
- [x] Record one affected `exam_id`, `attempt_id`, and `student_user_id` in this log without exposing credentials: `exam_id=000d0f30-78da-4ad7-b09a-2f41b078fd71`, `attempt_id=b32be245-3dfb-479e-8d13-733e5b3ce4de`, `student_id=2e782f06-6e6c-4b6f-baa7-7c4ecd05bfe3`.
- [x] Run the visibility SQL from the source context and record `is_public`, publication/status fields, both assignment representations, institution scope, and active enrollment scope.
- [x] Capture authenticated raw responses for `GET /api/exams` and `GET /api/exams/:id` and classify the visibility failure boundary.
- [x] Capture one browser telemetry request from the active web telemetry client, including event ID, dedupe key, attempt ID, student ID, HTTP status, and response body.
- [x] Record effective telemetry ingestion mode, telemetry settings, Redis queue/buffer stats, worker readiness/errors/stalls, and `flagged_incidents` write latency from the telemetry monitoring controller.
- [x] Verify whether production has the `flagged_incidents_dedupe_key_unique` index before changing incident persistence behavior.
- [ ] Capture audio phase, live-track state, model-load result, anomaly types, thresholds, cooldown, and top-class/confidence diagnostics for a speech/shouting reproduction.
- [ ] Capture answer-draft, stored-session, lobby-marker, attempt-status, reconnect, and admission state before and after reload/network loss/audio-track termination.
- [x] Verified a baseline telemetry mode test already exists in `app/sentinel-api/src/modules/telemetry/ingestion/services/ingestion-queue.service.test.ts`.
- [x] Verified a baseline student visibility SQL compilation test already exists in `app/sentinel-api/src/modules/examination/exams/data/get-exams.test.ts`.

**Migration required:** No. Diagnostics only.

**Stop condition:** Production-level evidence is not available from the repository alone, so the remaining diagnostics are blocked until the user supplies the affected identifiers or production access is available.
The affected identifiers are now recorded, so the next step is to run the visibility and telemetry diagnostics against the real environment when available.

## Current Findings

- Student visibility is enforced through shared SQL predicates in `app/sentinel-api/src/modules/examination/exams/data/build-student-exam-scope-predicates.ts`.
- Telemetry ingestion currently falls back to sync mode when Redis is requested without `REDIS_URL`, which is visible in `app/sentinel-api/src/modules/telemetry/ingestion/services/ingestion-queue.service.ts`.
- Telemetry health currently reports queue-facing stats, but the response shape is still focused on high-level ingestion availability in `app/sentinel-api/src/modules/telemetry/telemetry-monitoring.controller.ts`.
- The repository already contains dedicated tests for assignment visibility, telemetry mode fallback, incident persistence, and worker processing, which gives us a stable base for the next phase.
- Phase 0 visibility SQL was executed against the configured database on 2026-07-10. The affected exam currently resolves as `is_public = true`, `status = 'PUBLISHED'`, `published_at = 2026-07-10T12:42:28.898Z`, `class_group_id = a195fb04-650e-4413-b74d-ef6adb021bba`, `section_id = 6bacb7ef-6328-420a-83c2-e1c5962eb44f`, and `institution_id = 936953c8-547f-4377-90e5-b20836421212`. `exam_section_assignments` contains one classroom assignment row for the same classroom/section, `exam_assigned_sections` has no legacy rows, and the target `student_id = 2e782f06-6e6c-4b6f-baa7-7c4ecd05bfe3` currently has zero enrollment rows in this environment.
- That means the current database snapshot does not match the originally reported reproduction contract for a private classroom-assigned exam with an actively enrolled student. The immediate visibility issue is now classified as a database-state mismatch until authenticated API traces prove a different boundary.
- Authenticated raw API traces were captured with the configured student account against `https://api.sentinelph.tech`. `GET /exams` returned HTTP 200 and included `exam_id = 000d0f30-78da-4ad7-b09a-2f41b078fd71` in the response body; `GET /exams/000d0f30-78da-4ad7-b09a-2f41b078fd71` also returned HTTP 200 with the same exam in detail form. The current API boundary is therefore **not** “missing API row” and **not** “list/detail mismatch”. The affected record is visible at the API layer for this student account, and the current response shape shows a completed/turned-in attempt rather than a missing assigned exam.
- Production telemetry health was captured from `https://api.sentinelph.tech/telemetry/health` on 2026-07-10. The endpoint returned HTTP 200 with `ingestion.mode = 'redis'`, `queueName = 'telemetry-ingestion'`, `bufferName = 'telemetry-buffer'`, `waiting = 0`, `active = 0`, `failed = 0`, `completed = 0`, and `buffered = 0`. This confirms the public health surface currently reports Redis mode with an empty queue and buffer, but it does not by itself prove worker readiness logs, stalled-job history, or incident write latency.
- The configured database confirms `attempt_id = b32be245-3dfb-479e-8d13-733e5b3ce4de` is the telemetry `examSessionId` for this case. On 2026-07-10 the attempt row resolved as `started_at = 2026-07-10T12:43:42.534Z`, `completed_at = 2026-07-10T12:46:39.482Z`, `status = 'COMPLETED'`, `lifecycle_state = 'SUBMITTED'`, and `reconnect_attempt_count = 0`. That means the supplied attempt is no longer an active reproduction target for live telemetry-write latency or reload/recovery capture.
- The effective persisted telemetry settings row exists under `system_settings.setting_key = 'telemetry.global.settings'` with `updated_at = 2026-05-31T15:51:31.141Z`. Its current `operations` contract is `enabled = true`, `ingestionMode = 'redis'`, `batchingEnabled = true`, `batchWindowMs = 5000`, `maxBatchSize = 500`, and `dedupeWindowSeconds = 120`. The audio rule override currently sets `aiRules.audio_anomaly_detection.repeatThreshold = 3` and `confidenceThreshold = 0.85`.
- The target database already has `flagged_incidents_dedupe_key_unique` with definition `UNIQUE (attempt_id, rule_key, platform, dedupe_key) WHERE dedupe_key IS NOT NULL`, so no new dedupe migration is needed for the current environment.
- A real browser telemetry request was captured against `POST http://localhost:3001/telemetry/events` on 2026-07-10. The request body contained `examSessionId = d5e753c9-d415-4469-b256-af14ee6f98ae`, `studentId = 0374eb95-f83b-4a3b-9999-0ec6a32af1da`, `eventType = 'TAB_SWITCH'`, `platform = 'WEB'`, `source = 'CLIENT'`, `ruleKey = 'webSecurity.tab_switching_monitor'`, `metadata.eventId = 3c43c1ab-a8bb-41be-a816-a860a8aed2d0`, `metadata.dedupeKey = d5e753c9-d415-4469-b256-af14ee6f98ae:TAB_SWITCH:focus-loss:2026-07-10T15:23:25.000Z`, and `metadata.clientActionAt = 2026-07-10T15:23:25.889Z`. The endpoint returned accepted-for-processing with response body `{"message":"Event accepted for processing","data":null}`. This closes the “capture one browser telemetry request” requirement, but it still does not prove worker persistence or `flagged_incidents` write latency.
- The local database was queried for the exact captured telemetry dedupe key. The associated attempt currently resolves as `started_at = 2026-07-10T15:21:27.723Z`, `completed_at = 2026-07-10T15:24:28.712Z`, `status = 'COMPLETED'`, and `lifecycle_state = 'SUBMITTED'`. No `flagged_incidents` row exists for the exact dedupe key `d5e753c9-d415-4469-b256-af14ee6f98ae:TAB_SWITCH:focus-loss:2026-07-10T15:23:25.000Z`, so exact write latency for that specific request is still unproven.
- The persistence path is not globally broken in this environment: the same `attempt_id = d5e753c9-d415-4469-b256-af14ee6f98ae` already has persisted incident rows, including `TAB_SWITCH` at `2026-07-10T15:23:27.397Z` with dedupe key `d5e753c9-d415-4469-b256-af14ee6f98ae:TAB_SWITCH:focus-loss:2026-07-10T15:21:36.000Z`, `RIGHT_CLICK_ATTEMPT` rows at `2026-07-10T15:23:10.891Z` and `2026-07-10T15:23:23.004Z`, `CLIPBOARD_ATTEMPT` at `2026-07-10T15:23:12.237Z`, and `FULL_SCREEN_EXIT` at `2026-07-10T15:24:29.109Z`. This narrows the remaining telemetry boundary to worker/queue observability, event bucketing, or dedupe correlation for the exact captured request rather than total persistence failure.
- A direct shell request to `http://localhost:3001/telemetry/health` from this workspace failed to connect during this pass, so worker readiness/errors/stalls still need either the running API terminal output or a successful health response captured from the active environment before the Phase 0 telemetry observability task can be closed.
- The telemetry settings and queue stats were then resolved directly from the app services against the configured environment on 2026-07-10. `telemetrySettingsResolverService.resolve()` returned the persisted `telemetry.global.settings` row with `updatedBy = 'Sentinel Support'`, `updatedAt = 2026-05-31T15:51:31.141Z`, and the same Redis-mode operations contract already recorded. `telemetryIngestionQueueService.getStats()` returned `mode = 'redis'`, `queueName = 'telemetry-ingestion'`, `bufferName = 'telemetry-buffer'`, `waiting = 0`, `active = 0`, `failed = 0`, `completed = 171`, and `buffered = 0`. Redis configuration validation also succeeded (`maxmemory-policy = noeviction`), which is strong evidence that the configured queue path is reachable and has processed jobs in this environment.
- A live local health capture from `curl http://localhost:3001/telemetry/health` was recorded on 2026-07-10 at `2026-07-10T15:33:17.880Z` and returned `{"status":"ok","timestamp":"2026-07-10T15:33:17.880Z","ingestion":{"mode":"redis","queueName":"telemetry-ingestion","bufferName":"telemetry-buffer","waiting":0,"active":0,"failed":0,"completed":171,"buffered":0}}`. This matches the direct service-level queue stats and confirms the running API surface is reporting the same Redis ingestion state.
- With explicit user approval, `pnpm start:telemetry-worker` was started briefly on 2026-07-10 and then stopped immediately after readiness capture. The worker output showed `[TelemetryWorker] Validating Redis configuration...`, `[Redis] Config GET result: ["maxmemory-policy","noeviction"]`, and `[TelemetryWorker] Worker ready { queueName: 'telemetry-ingestion', concurrency: 5, redisHost: '127.0.0.1', redisPort: '6379' }`, followed by clean shutdown via `SIGINT`. This closes the worker-readiness portion of the telemetry observability evidence without leaving the worker running.
- Stored `flagged_incidents.details` provided the exact write-latency correlation for the captured browser event. The persisted `TAB_SWITCH` incident row `incident_id = 18cabd6a-eb9c-41ca-a637-bc2527473d49` has row `timestamp = 2026-07-10T15:23:27.397Z`, while its embedded `details.metadata.eventId = 3c43c1ab-a8bb-41be-a816-a860a8aed2d0`, `details.metadata.dedupeKey = d5e753c9-d415-4469-b256-af14ee6f98ae:TAB_SWITCH:focus-loss:2026-07-10T15:23:25.000Z`, and `details.metadata.clientActionAt = 2026-07-10T15:23:25.889Z` exactly match the captured browser request. Using the stored row timestamp minus `clientActionAt`, the observed write latency for that event was approximately **1.508 seconds**. Note that the row-level `flagged_incidents.dedupe_key` remained on the older bucket key `d5e753c9-d415-4469-b256-af14ee6f98ae:TAB_SWITCH:focus-loss:2026-07-10T15:21:36.000Z` because the persistence service updated an existing in-window incident record and preserved the original row identity while recording the newer event under `details.lastEvent`.
- A follow-up attempt to briefly start the telemetry worker only for readiness-log capture was blocked by policy because starting the worker could process queued jobs and mutate live incident state. Because of that boundary, the remaining missing evidence for the Phase 0 telemetry observability task is specifically direct worker liveness output (`ready`, `error`, `failed`, or `stalled`) from an already-running environment, not queue/settings reachability.
- A direct production mutation attempt to submit a synthetic telemetry event for the real student attempt was blocked by policy because it could create or alter incident records for a real user. Because of that safety boundary, the log still lacks a captured production request/response pair from an active browser client and still lacks measured `flagged_incidents` write latency for this attempt.
- Read-only audio and recovery diagnostics confirm the current static defaults and state gates, but not a live reproduction: `DEFAULT_AUDIO_ANOMALY_CONFIG` enables only `TALKING` and `BACKGROUND_NOISE`, uses `cooldownMs = 10000`, `consecutiveFrameThreshold = 2`, and thresholds `{ TALKING: 0.45, BACKGROUND_NOISE: 0.55, TYPING: 0.8, TAPPING: 0.8, MOUTH_BREATHING: 0.6, SILENCE_DETECTED: 0.015 }`. `useExamSession` clears stale session state and routes non-startable/non-resumable students back to lobby, while `useLobbyState` treats resumable attempts and approved admissions as the only paths that can enter the exam. The missing part is still live before/after state capture across reload, network loss, and ended-track recovery.
- Manual browser evidence from the student checkup flow was captured on 2026-07-11 from `http://localhost:3000/student/exam/f32d59f9-0457-40a4-bc48-dbf089dd3d71/checkup` and `.../lobby` using the user-provided screenshots. Before reload, the checkup page showed `Camera = READY`, `Microphone = READY`, and `Identity Scan = PENDING` while MediaPipe calibration was active (`Holding Position`, about `83%`) with the face mesh overlay visible. After reload, the same checkup route regressed to `Camera = PENDING`, `Microphone = PENDING`, and `Identity Scan = PENDING`, restored the `Grant Device Permissions` button, and removed the live preview. The Network panel at that point showed repeated `face_landmarker.task` fetches plus heartbeat/health traffic, which means the MediaPipe/model boot path restarted but the device-readiness state did not survive the reload.
- The same manual capture also showed that the student could still reach the lobby after the earlier successful checkup path. The lobby page for `http://localhost:3000/student/exam/f32d59f9-0457-40a4-bc48-dbf089dd3d71/lobby` displayed `Current readiness: 2/2 completed`, `Reconnect attempts used: 0 of 3. Remaining: 3.`, and `Exam state: Waiting for approval`, with the primary action disabled as `Waiting for Approval`. This confirms that the persisted flow marker used by the lobby can remain truthy even when the checkup page itself falls back to a fresh permission/calibration state after reload.
- Root-cause classification from the current code matches that browser evidence. `checkup/page.tsx` computes readiness from live in-memory device state returned by `useStudentCheckupManager()` (`cameraState`, `micState`, `isStreamActive`) plus live MediaPipe calibration state from `useCheckupMediaPipe()`. Those permission states initialize as `idle` on mount and are only promoted by fresh `getUserMedia()` calls in `student-exam-mediapipe-provider.tsx` and `student-exam-audio-provider.tsx`; they are not restored from session storage. By contrast, the lobby only checks stored flow markers from `patchStoredStudentExamFlow()` / `readStoredStudentExamFlow()` (`privacyAccepted`, `checkupCompleted`, MediaPipe activation timestamps). So after a reload, the UI can simultaneously show (a) lobby readiness still completed from stored session flow and (b) checkup permission cards reset back to pending because camera/microphone stream state is not durably rehydrated.
- This narrows the remaining Phase 0 issue from “microphone never works” to a more specific contract gap: the checkup route treats device readiness as ephemeral live-stream state, while downstream lobby gating treats checkup completion as persisted session-flow state. The screenshots do **not** yet prove a bad microphone capture pipeline or a bad YAMNet/audio anomaly decision; they prove a readiness-state mismatch across reload boundaries.
- A direct live reproduction through Chrome on 2026-07-11 tightened that root cause further. Starting from a fully ready checkup state (`Camera READY`, `Microphone READY`, `Identity Scan READY`), a page reload first showed the checkup route still ready while the page was settling, then after the route fully reinitialized it regressed to `Grant Device Permissions` with `Camera PENDING`, `Microphone PENDING`, and `Identity Scan PENDING`. Immediately navigating from that regressed checkup page to `/lobby` in the same session then showed `Exam state: Pending checks`, `Current readiness: 0 / 2 completed`, `Return to checkup first so MediaPipe can finish activation before the attempt starts.`, and a disabled `Continue to Attempt` button. In the same Network panel, the reload/lobby transition showed `heartbeat`, `health`, `GET /exams/:id`, `GET /configuration/exams/:id`, and a fresh `face_landmarker.task` fetch.
- That means the current reload bug is not only “live permission state is lost.” `checkup/page.tsx` also persists the loss back into session storage on every render through `patchStoredStudentExamFlow(examId, { checkupCompleted: isCheckupReady, mediaPipeActivatedAt: hasCompletedMediaPipeActivation ? ... : null, mediaPipeCalibrationCompletedAt: hasCompletedMediaPipeActivation ? ... : null, mediaPipeActivationSource: hasCompletedMediaPipeActivation ? 'checkup' : null, mediaPipeCalibrationProfile: hasCompletedMediaPipeActivation ? calibrationProfile : null })`. When the page remounts after reload with default `idle` permission state and no active calibration yet, that effect rewrites the previously good flow marker to `checkupCompleted = false` and clears the MediaPipe activation timestamps/profile before the user has a chance to re-acquire the stream. The lobby then reads that downgraded state through `readStoredStudentExamFlow()` / `useLobbyReadiness()` and correctly blocks entry with `0 / 2 completed`.

## Phase 2: Durable Production Telemetry Pipeline

- [x] Re-checked `app/sentinel-api/src/modules/telemetry/ingestion/services/ingestion-queue.service.ts` against the live worker/queue evidence and the expanded queue-service tests. No source change was required in this pass because the captured behavior showed accepted jobs remain visible in Redis (`waiting = 1`) when the worker is offline, successful flushes clear only the snapshot after persistence, and failed flushes preserve the snapshot for recovery instead of silently dropping buffered events.
- [x] Expanded `app/sentinel-api/src/modules/telemetry/telemetry-monitoring.controller.ts` so telemetry health responses include `bufferName` and `buffered` in addition to the queue counts already returned by `TelemetryIngestionQueueService.getStats()`.
- [x] Added `app/sentinel-api/src/modules/telemetry/telemetry-monitoring.controller.test.ts` to verify the health route exposes queue and buffer stats in the JSON payload.
- [x] Ran targeted Vitest coverage for `app/sentinel-api/src/modules/telemetry/telemetry-monitoring.controller.test.ts`, `app/sentinel-api/src/modules/telemetry/ingestion/services/ingestion-queue.service.test.ts`, and `app/sentinel-api/src/modules/telemetry/ingestion/services/telemetry-job-processor.service.test.ts`; all passed.
- [x] Ran targeted Vitest coverage for `app/sentinel-api/src/modules/examination/exams/data/build-student-exam-scope-predicates.test.ts` and `app/sentinel-api/src/modules/examination/exams/data/get-exams.test.ts`; all passed.
- [x] Added focused monitoring contract coverage in `app/sentinel-api/src/modules/examination/monitoring/services/get-exam-monitoring-overview.test.ts` and `get-exam-monitoring-student-detail.test.ts`, proving the API response surfaces committed incident summary fields (`incidentCount`, `openIncidentCount`, `latestIncidentType`, flagged status) and student-detail incident/lifecycle data from persisted monitoring records.
- [x] Ran targeted Vitest coverage for `app/sentinel-api/src/modules/examination/monitoring/services/get-exam-monitoring-overview.test.ts` and `get-exam-monitoring-student-detail.test.ts`; both tests passed on 2026-07-11.
- [x] Added `docs/task/2026-07-10/fix-002-telemetry-worker-runbook.md` documenting the production worker start command, required API/worker environment alignment, queue/job/buffer names, database credential dependency, restart policy, flush authorization, alert thresholds, and manual recovery steps.
- [x] Expanded `app/sentinel-api/src/modules/telemetry/ingestion/services/ingestion-queue.service.test.ts` so the queue-service suite now covers sync persistence, Redis enqueue, Redis queue/buffer stats, Redis batch buffering, successful snapshot flush, and failed-flush snapshot preservation for recovery.
- [x] Updated `app/sentinel-api/src/modules/telemetry/ingestion/workers/telemetry.worker.ts` and `telemetry-job-processor.service.ts` so failed, stalled, and terminal-dropped telemetry jobs now log the concrete reconciliation identifiers (`attemptId`, `studentId`, `eventType`, `ruleKey`, `timestamp`, `eventId`, `dedupeKey`) instead of only queue-level metadata.
- [x] Added focused coverage in `app/sentinel-api/src/modules/telemetry/ingestion/workers/telemetry.worker.test.ts` and extended `telemetry-job-processor.service.test.ts` to verify inactive-mode exit, failed-job logging, stalled-job lookup/logging, and terminal-drop logging with reconciliation identifiers.
- [x] Re-verified the existing `20260706000000_add_telemetry_dedupe_index` rollout requirement against the target environment evidence: the database already contains `flagged_incidents_dedupe_key_unique`, so no additional migration apply step was needed in this pass.
- [x] Ran targeted Vitest coverage for `app/sentinel-api/src/modules/telemetry/ingestion/services/ingestion-queue.service.test.ts`, `app/sentinel-api/src/modules/telemetry/ingestion/controllers/flush-telemetry.controller.test.ts`, `app/sentinel-api/src/modules/examination/monitoring/services/get-exam-monitoring-overview.test.ts`, and `get-exam-monitoring-student-detail.test.ts`; all 16 tests passed on 2026-07-11.
- [x] Ran targeted Vitest coverage for `app/sentinel-api/src/modules/telemetry/ingestion/services/telemetry-job-processor.service.test.ts` and `app/sentinel-api/src/modules/telemetry/ingestion/workers/telemetry.worker.test.ts`; all 8 tests passed on 2026-07-11.
- [x] After explicit user approval for live execution, ran a production-like worker test against the current `.env` stack (`TELEMETRY_INGESTION_MODE=redis`, `REDIS_URL=rediss://uncommon-grouper-118173.upstash.io:6379`, `DATABASE_URL` on `aws-1-ap-northeast-1.pooler.supabase.com:6543`). Starting `pnpm --dir app/sentinel-api start:telemetry-worker` produced `[TelemetryWorker] Worker ready { queueName: 'telemetry-ingestion', concurrency: 5, redisHost: 'uncommon-grouper-118173.upstash.io', redisPort: '6379' }`.
- [x] Submitted one controlled live queue job with `attemptId = 00000000-0000-4000-8000-000000000001`, `studentId = 00000000-0000-4000-8000-000000000002`, `eventType = TAB_SWITCH`, `eventId = 76f13105-9a88-4270-871a-69be077fc8c0`, and `dedupeKey = codex-phase2-live-1783738472862`. Immediate queue stats after submission were `waiting = 1`, `active = 0`, `failed = 0`, `completed = 0`, `buffered = 0`.
- [x] Captured the live worker outcome for that job. The worker logged `[TelemetryWorker] Dropping terminal telemetry job.` with `status = 404`, message `Exam session not found for telemetry ingestion.`, and the full reconciliation identifiers (`attemptId`, `studentId`, `eventType`, `ruleKey`, `timestamp`, `eventId`, `dedupeKey`). Post-processing queue stats were `waiting = 0`, `active = 0`, `failed = 0`, `completed = 1`, `buffered = 0`.
- [x] Stopped the worker with `SIGINT`, then submitted a second controlled job while the worker was offline using `attemptId = 00000000-0000-4000-8000-000000000011`, `studentId = 00000000-0000-4000-8000-000000000012`, `eventId = 9c7c2b5a-5620-4549-9af3-32a6e5f336b0`, and `dedupeKey = codex-phase2-offline-1783738528673`. Queue stats immediately after submission were `waiting = 1`, `active = 0`, `failed = 0`, `completed = 1`, `buffered = 0`, which proves the accepted event remained queued while no worker was running and was not manually deleted.
- [x] Restarted the worker briefly for cleanup. The queued offline job was then terminal-dropped with the same full reconciliation identifiers, and final queue stats returned to `waiting = 0`, `active = 0`, `failed = 0`, `completed = 2`, `buffered = 0` before the worker was shut down again with `SIGINT`.

**Migration required:** No. This telemetry health expansion is additive and does not alter the ingestion schema.

**Breaking changes:** No. Existing consumers can ignore the new `bufferName` and `buffered` fields.

## Phase 1: Private Assigned Exam Visibility Invariant

- [x] Re-checked the Phase 1 baseline classification against the recorded diagnostics: the captured environment did **not** show missing `exam_section_assignments` rows, stale `exam_assigned_sections` rows, a list/detail predicate mismatch, or a student-UI filtering bug. The current reproduced boundary remains a database-state mismatch (`exam_id = 000d0f30-78da-4ad7-b09a-2f41b078fd71` is public in the inspected environment and the target `student_id = 2e782f06-6e6c-4b6f-baa7-7c4ecd05bfe3` has zero enrollments there).
- [x] Verified `app/sentinel-api/src/modules/examination/exams/services/create-exam.service.ts` already persists `exam_section_assignments` transactionally for classroom-targeted exams and preserves assignment rows when the write path does not change classroom targeting.
- [x] Verified `app/sentinel-api/src/modules/examination/exams/services/update-exam.service.ts` already preserves assignment rows and summary sync when only `isPublic` changes, and only rewrites assignment rows when assignment-relevant fields actually change.
- [x] Verified `app/sentinel-api/src/modules/examination/exams/data/build-student-exam-scope-predicates.ts` already encodes the intended private/public assignment rules: exact classroom enrollment, explicit assignment-table links, archived-classroom rejection, legacy section fallback, and remediation handling.
- [x] Verified `app/sentinel-api/src/modules/examination/exams/data/get-exams.ts` and `get-exam-by-id.ts` already apply the same published-plus-assignment student visibility gates for list and detail queries.
- [x] Verified runtime/lobby entry uses the same assignment context through `app/sentinel-api/src/modules/examination/access/access.service.ts` and `evaluate-student-exam-eligibility.service.ts`, where `EntitlementsRepository.hasStudentExamEnrollment(...)` receives the merged `classGroupId`, `subjectId`, `sectionId`, and `assigned_section_ids` context for runtime admission checks.
- [x] Verified the student web normalization layer already preserves assigned private/published exams rather than filtering them out in `app/sentinel-web/src/app/(protected)/student/exam/_hooks/use-exam-list.test.ts` and `app/sentinel-web/src/app/(protected)/student/history/_hooks/use-student-history/index.test.ts`.
- [x] Ran focused API Vitest coverage for `app/sentinel-api/src/modules/examination/exams/services/create-exam.service.test.ts`, `update-exam.service.test.ts`, `build-student-exam-scope-predicates.test.ts`, `get-exams.test.ts`, `get-exam-by-id.test.ts`, and `app/sentinel-api/src/modules/examination/access/access.test.ts`; all 55 tests passed.
- [x] Ran focused web Vitest coverage for `app/sentinel-web/src/app/(protected)/student/exam/_hooks/use-exam-list.test.ts` and `app/sentinel-web/src/app/(protected)/student/history/_hooks/use-student-history/index.test.ts`; all 10 tests passed.

**Migration required:** No. The currently captured evidence did not require a Phase 1 schema change or data remediation in this local pass.

**Breaking changes:** None. No Phase 1 application code changed in this verification pass; the existing visibility contract was re-verified with focused tests.

## Phase 3: Event Integrity and Turn-In Semantics

- [x] Verified `app/sentinel-web/src/app/(protected)/student/exam/[id]/attempt/_hooks/use-student-exam-attempt/use-attempt-submission.ts` suspends monitoring before navigation and fullscreen teardown, matching the intended turn-in ordering.
- [x] Verified `app/sentinel-web/src/app/(protected)/student/exam/[id]/_hooks/use-exam-monitoring/use-interaction-listeners.ts` suppresses fullscreen telemetry during `submitting`, `navigating-to-turn-in`, and `suspended` phases.
- [x] Ran focused web Vitest coverage for `app/sentinel-web/src/app/(protected)/student/exam/[id]/attempt/_hooks/use-student-exam-attempt/use-attempt-submission.test.tsx`, `app/sentinel-web/src/app/(protected)/student/exam/[id]/attempt/_hooks/use-student-exam-attempt/index.test.tsx`, and `app/sentinel-web/src/app/(protected)/student/exam/[id]/_hooks/use-exam-monitoring.test.ts`; all passed.
- [x] Reproduced the fullscreen turn-in boundary from automated browser-facing coverage. `attempt/page.test.tsx` proves route navigation to `/result` starts before `document.exitFullscreen()`, and an immediate `fullscreenchange` during turn-in does not create a security lock or turn-in interruption. `use-exam-monitoring.test.ts` proves `FULL_SCREEN_EXIT` telemetry is suppressed in `submitting`, `navigating-to-turn-in`, and `suspended` phases, so the current browser-side classification is “no post-completion fullscreen payload should be emitted.”
- [x] Verified Phase 3 did **not** require a new submission-hook or monitoring-hook patch. The ordering already matched the intended contract, so no additional code change was needed for the “if ordering is incorrect” branch.
- [x] Added direct unit coverage in `app/sentinel-api/src/modules/telemetry/storage/services/incident-session-eligibility.service.test.ts` proving active-attempt `FULL_SCREEN_EXIT` remains eligible while completed-attempt `FULL_SCREEN_EXIT` is returned as `IGNORE_SILENTLY`.
- [x] Re-verified the persistence contract in the existing DB-backed suite `app/sentinel-api/src/modules/telemetry/storage/services/incident-persistence.service.test.ts`: it already covers first occurrence creation, exact duplicate dedupe-key no-op, same-rule/window occurrence increment, outside-window new incident creation, completed-attempt fullscreen suppression, and active-attempt fullscreen persistence.
- [x] Re-verified that `app/sentinel-api/src/modules/telemetry/storage/mappers/insert-incident.mapper.ts` did not need a Phase 3 change. The current baseline already distinguishes exact duplicates by `dedupeKey` while still aggregating later same-rule events inside the configured window.
- [x] Ran focused API Vitest coverage for `app/sentinel-api/src/modules/telemetry/storage/services/incident-session-eligibility.service.test.ts`; 2 tests passed on 2026-07-11.
- [x] Ran focused web Vitest coverage for `app/sentinel-web/src/app/(protected)/student/exam/[id]/attempt/_hooks/use-student-exam-attempt/use-attempt-submission.test.tsx` and `app/sentinel-web/src/app/(protected)/student/exam/[id]/_hooks/use-exam-monitoring.test.ts`; all 37 tests passed on 2026-07-11.
- [x] Attempted to rerun the DB-backed incident persistence suite for Phase 3 evidence. The suite is currently blocked by environment connectivity (`Can't reach database server at aws-1-ap-northeast-1.pooler.supabase.com`), but the existing test file already contains the required assertions for completed-attempt suppression, exact duplicate no-op, same-window aggregation, and outside-window new row creation.
- [x] No staff-surface parity tests were required in this phase because Phase 3 did not change instructor/admin monitoring presentation fields or lifecycle badge rendering.

**Migration required:** No new migration for the verified web-side event-ordering behavior.

**Breaking changes:** No user-facing API change; the confirmed behavior prevents post-submit fullscreen incidents from being emitted.

## Phase 5: Attempt Recovery and Lobby Reconciliation

- [x] Documented the current state machine from the shared schemas and runtime-access helpers before changing names. Attempt lifecycle enums are `IN_PROGRESS`, `LOCKED`, `CLOSED`, `SUBMITTED`, and `SUPERSEDED`; score-state enums are `DRAFT`, `FINALIZED`, and `REVISION_REQUIRED`; runtime-access states surfaced across student and staff flows are `before_start`, `open`, `lobby_waiting`, `lobby_approved`, `locked`, `reopened`, and `closed`. In practice the recovery path is: stored session or active attempt → runtime access permits `canResume` → attempt resumes; missing/stale marker or blocked runtime access → redirect to lobby; instructor-gated lobby remains `lobby_waiting` until approved; terminal attempts surface as `closed`/`superseded`; reopened attempts surface as `reopened`; in-attempt incident or policy actions surface as `locked`.
- [x] Verified `app/sentinel-api/src/modules/examination/flow/data/session.repository.ts` preserves and resumes active attempts, increments reconnect counts, and blocks completed attempts from being restarted.
- [x] Verified `app/sentinel-api/src/modules/examination/flow/services/start-session.service.ts` returns resumed-session data and emits the correct session-started versus session-resumed branch.
- [x] Verified `app/sentinel-web/src/app/(protected)/student/exam/[id]/_hooks/use-exam-session/index.ts` routes missing markers and non-resumable states back to the lobby while keeping valid stored sessions intact.
- [x] Verified `app/sentinel-web/src/app/(protected)/student/exam/[id]/lobby/_hooks/use-lobby-state.ts` keeps approved or resumable students in the correct lobby flow and polls instructor admission only when needed.
- [x] Updated `app/sentinel-web/src/app/(protected)/student/exam/[id]/checkup/page.tsx` and `app/sentinel-web/src/app/(protected)/student/exam/[id]/_lib/student-exam-flow/index.ts` so a previously completed checkup is preserved across reload, temporary mount-time `idle` permission state no longer overwrites `checkupCompleted` in session storage, and an explicit device-access retry intentionally clears the stored activation before rerunning the checkup.
- [x] Added focused coverage in `app/sentinel-web/src/app/(protected)/student/exam/[id]/checkup/page.test.tsx` proving reload preserves a previously completed checkup and proving a new device-access request clears the stored readiness contract before rerunning permissions.
- [x] Ran focused web Vitest coverage for `app/sentinel-web/src/app/(protected)/student/exam/[id]/_hooks/use-exam-session.test.tsx`, `app/sentinel-web/src/app/(protected)/student/exam/[id]/lobby/_hooks/use-lobby-state.test.tsx`, and `app/sentinel-web/src/app/(protected)/student/exam/[id]/attempt/_hooks/use-student-exam-attempt/index.test.tsx`; all passed.
- [x] Ran focused API Vitest coverage for `app/sentinel-api/src/modules/examination/flow/data/session.repository.test.ts`, `app/sentinel-api/src/modules/examination/flow/flow.test.ts`, and `app/sentinel-api/src/modules/examination/runtime-access/runtime-access.service.test.ts`; all passed.
- [x] Re-ran focused web Vitest coverage for `app/sentinel-web/src/app/(protected)/student/exam/[id]/checkup/page.test.tsx`, `app/sentinel-web/src/app/(protected)/student/exam/[id]/lobby/page.test.tsx`, and `app/sentinel-web/src/app/(protected)/student/exam/[id]/lobby/_hooks/use-lobby-state.test.tsx`; all passed after the reload-preservation patch.
- [x] Fixed the stale mock-path mismatch in `app/sentinel-api/src/modules/examination/lobby/lobby.service.test.ts` so `assertInstructorExamAccess` is mocked from the same module path that `LobbyService` imports; re-ran the focused API suite and all 7 tests passed.
- [x] Re-verified the recovery teardown/startup contract. `useAttemptMonitoring.ts` suspends browser security monitoring and passes `isSuspended: isRedirectingToTurnIn` into `useAudioAnomalyWorker`, so audio monitoring is stopped before turn-in teardown; the hook only calls `ensureAudioAccess(configuration)` when the attempt is active and not redirecting, which keeps fresh monitoring startup behind a valid attempt state plus a live/managed audio stream. Existing `use-attempt-submission.test.tsx`, `use-student-exam-attempt/index.test.tsx`, and the Phase 4 audio hook tests cover the suspension-before-navigation and recovery-routing behavior.
- [x] Re-verified the API recovery coverage with focused suites. `flow.test.ts` already covers resumed sessions, lobby-based reconnects, and lobby-waiting blocks; `access.test.ts` already covers `lobby_waiting`, `lobby_approved`, closed/superseded access boundaries, and replacement-attempt eligibility; `runtime-access.service.test.ts` already covers before-start, locked, reopened, open, and post-cutoff resumable states; `lobby.service.test.ts` covers student-path admission polling and instructor-gated mutation/list access.
- [x] Re-verified the staff-surface recovery visibility using the existing monitoring components plus expanded badge suites. `monitoring-header.tsx` in both web and core already surfaces `Lobby Waiting`, `Lobby Approved`, `Reopened`, `Locked`, and `Closed` runtime-access labels, while `attempt-lifecycle-badge.test.tsx` in both apps now proves `LOCKED`, `CLOSED`, `SUPERSEDED`, `FINALIZED`, `REVISION REQUIRED`, and `DRAFT` remain visibly distinct.
- [x] Ran focused API Vitest coverage for `app/sentinel-api/src/modules/examination/runtime-access/runtime-access.service.test.ts`, `app/sentinel-api/src/modules/examination/lobby/lobby.service.test.ts`, `app/sentinel-api/src/modules/examination/flow/flow.test.ts`, and `app/sentinel-api/src/modules/examination/access/access.test.ts`; all 40 tests passed on 2026-07-11.
- [x] Ran focused web Vitest coverage for `app/sentinel-web/src/app/(protected)/student/exam/[id]/_hooks/use-exam-session.test.tsx`, `app/sentinel-web/src/app/(protected)/student/exam/[id]/lobby/_hooks/use-lobby-state.test.tsx`, `app/sentinel-web/src/app/(protected)/student/exam/[id]/lobby/page.test.tsx`, `app/sentinel-web/src/app/(protected)/student/exam/[id]/attempt/_hooks/use-student-exam-attempt/index.test.tsx`, and `app/sentinel-web/src/features/exams/monitoring/_components/attempt-lifecycle-badge.test.tsx`; all 36 tests passed on 2026-07-11.
- [x] Ran focused core Vitest coverage for `app/sentinel-core/src/features/exams/monitoring/_components/attempt-lifecycle-badge.test.tsx`; both tests passed on 2026-07-11.

**Migration required:** No. The recovery behavior is already represented by the existing `exam_attempts` lifecycle fields and lobby tables.

**Breaking changes:** No API shape change; the confirmed behavior preserves answers, reconnect counts, and lobby gating across reload/re-entry.

## Phase 4: Audio Lifecycle, Calibration, and Recovery

- [x] Verified `app/sentinel-web/src/app/(protected)/student/exam/[id]/_components/student-exam-audio-provider.tsx` treats ended tracks as inactive and re-requests audio access when the stream is no longer live.
- [x] Verified `app/sentinel-web/src/hooks/use-audio-anomaly-worker/audio-anomaly-controller.ts` handles worker start, timeout, capability failure, and cleanup with distinct phases.
- [x] Verified `app/sentinel-web/src/hooks/use-audio-anomaly-worker/use-audio-anomaly-worker.ts` keeps anomaly emission suspended when the hook is suspended and emits telemetry only for accepted anomalies.
- [x] Verified shared audio mapping/configuration in `packages/shared/src/audio/audio-anomaly.ts` and `packages/shared/src/audio/yamnet-class-mapper.ts` still aligns class IDs, anomaly types, and default thresholds.
- [x] Ran focused web Vitest coverage for `app/sentinel-web/src/app/(protected)/student/exam/[id]/_components/student-exam-audio-provider.test.tsx` and `app/sentinel-web/src/hooks/use-audio-anomaly-worker/use-audio-anomaly-worker.test.tsx`; all passed.
- [x] Re-checked `app/sentinel-web/src/workers/audio-anomaly-engine.ts` against the existing engine and integration fixtures. No engine logic change was required in this pass: the current tests already cover 48kHz→16kHz resampling, configured threshold gating, disabled keyboard anomalies, enabled keyboard anomalies, strongest-anomaly selection, RMS-based background-noise fallback, and silence remaining opt-in by default.
- [x] Re-checked `packages/shared/src/audio/yamnet-class-mapper.ts` and `packages/shared/src/audio/audio-anomaly.ts` against the shared mapper/unit suites. The current class ID map and default thresholds already align with the intended contract, so no rename or threshold remapping was required in this pass.
- [x] Added additive audio diagnostics metadata to `app/sentinel-web/src/hooks/use-audio-anomaly-worker/use-anomaly-telemetry.ts` and the shared telemetry schema: audio telemetry now includes `audioDiagnostics.threshold`, `audioDiagnostics.configVersion`, `audioDiagnostics.workerPhase`, and `audioDiagnostics.streamPhase` alongside the existing `anomalyType`, `confidenceScore`, `eventId`, and `dedupeKey` fields.
- [x] Fixed the hook-level regression introduced while adding audio diagnostics by switching the telemetry helper to read `runtimeConfig`, worker phase, and stream state from refs instead of effect dependencies. This preserves the existing controller lifecycle, prevents spurious re-initialization, and records the correct live worker phase at anomaly time.
- [x] Ran focused API Vitest coverage for `app/sentinel-api/src/modules/telemetry/ingestion/ingestion.dto.test.ts` and `app/sentinel-api/src/modules/telemetry/ingestion/rules/ai-rules.test.ts`; all 10 tests passed on 2026-07-11.
- [x] Ran focused web Vitest coverage for `app/sentinel-web/src/hooks/use-audio-anomaly-worker/use-audio-anomaly-worker.test.tsx`, `app/sentinel-web/src/workers/tests/audio-anomaly-engine.test.ts`, `app/sentinel-web/src/workers/tests/audio-anomaly.integration.test.ts`, and `app/sentinel-web/src/lib/audio/audio-phase0.test.ts`; all 36 tests passed on 2026-07-11.
- [x] Ran focused shared Vitest coverage for `packages/shared/src/audio/yamnet-class-mapper.test.ts`; all 9 tests passed on 2026-07-11.
- [x] The DB-backed telemetry persistence suite in `app/sentinel-api/src/modules/telemetry/storage/services/incident-persistence.service.test.ts` remains environment-blocked because the configured database host is unreachable, but the existing suite already contains the required audio anomaly metadata and audio dedupe assertions (`anomalyType`, `confidenceScore`, distinct audio `dedupeKey` increment, duplicate `dedupeKey` no-op).

**Migration required:** No. The audio lifecycle behavior is implemented using existing telemetry metadata and shared audio thresholds.

**Breaking changes:** No user-facing API change; the new audio diagnostic metadata is additive and backward-compatible.

## Phase 6: Cross-Surface Contract Validation and Release Readiness

- [x] Ran focused API Vitest coverage for assignment visibility, telemetry queue/worker, monitoring overview/detail, and recovery services using `create-exam.service.test.ts`, `update-exam.service.test.ts`, `build-student-exam-scope-predicates.test.ts`, `get-exams.test.ts`, `get-exam-by-id.test.ts`, `access.test.ts`, `ingestion-queue.service.test.ts`, `telemetry-job-processor.service.test.ts`, `telemetry.worker.test.ts`, `telemetry-monitoring.controller.test.ts`, `get-exam-monitoring-overview.test.ts`, `get-exam-monitoring-student-detail.test.ts`, `runtime-access.service.test.ts`, `lobby.service.test.ts`, and `flow.test.ts`; all 97 tests passed on 2026-07-11.
- [x] Ran focused web/shared Vitest coverage for student list/history normalization, submission/fullscreen monitoring, audio worker/engine, session/lobby recovery, and monitoring UI using `use-exam-list.test.ts`, `use-student-history/index.test.ts`, `use-attempt-submission.test.tsx`, `use-exam-monitoring.test.ts`, `use-audio-anomaly-worker.test.tsx`, `audio-anomaly-engine.test.ts`, `audio-anomaly.integration.test.ts`, `audio-phase0.test.ts`, `use-exam-session.test.tsx`, `use-lobby-state.test.tsx`, `lobby/page.test.tsx`, `use-student-exam-attempt/index.test.tsx`, `attempt-lifecycle-badge.test.tsx`, and `packages/shared/src/audio/yamnet-class-mapper.test.ts`; all 128 tests passed on 2026-07-11.
- [x] Investigated the live student classroom visibility failure reported on 2026-07-11 for exam `50a36d57-c418-42e2-88bb-879dc6a08c44` and classroom `a195fb04-650e-4413-b74d-ef6adb021bba`. Direct database checks proved the exam is `PUBLISHED`, `is_public = false`, linked to the exact classroom through both `exams.class_group_id` and `exam_section_assignments`, and the target student account `0374eb95-f83b-4a3b-9999-0ec6a32af1da` is actively enrolled through `student_id = 2e782f06-6e6c-4b6f-baa7-7c4ecd05bfe3`.
- [x] Reproduced the effective root cause at the code boundary: the live student token claims `user_metadata.role = "authenticated"`. `resolveAssessmentActorRole(...)` treated that generic claim as authoritative and returned early, so the assessment scope never normalized to `student` even though the database-backed student profile existed. `GET /exams` and `GET /exams/:id` then remained permission-gated against a non-student role, and the student classroom/history UIs masked the resulting read failure behind their normal empty states.
- [x] Patched `app/sentinel-api/src/modules/examination/assessment/assessment-access.ts` so generic claims (`authenticated`, `anon`, `anonymous`) fall through to the existing DB-backed role/profile resolution, preserving real `student` and `instructor` roles. Kept the controller ordering fix in `app/sentinel-api/src/modules/examination/exams/controllers/get-exams.controller.ts` and `get-exam.controller.ts` so confirmed student viewers bypass the staff permission gate only after scope resolution.
- [x] Added regression coverage in `assessment-access.test.ts`, `get-exams.controller.test.ts`, and `get-exam.controller.test.ts`, then ran `pnpm --dir app/sentinel-api exec vitest run src/modules/examination/assessment/assessment-access.test.ts src/modules/examination/exams/controllers/get-exams.controller.test.ts src/modules/examination/exams/controllers/get-exam.controller.test.ts` with 28/28 tests passing on 2026-07-11.
- [x] Continued the live investigation after the generic-claim fix and found the remaining student visibility bug was a mixed-role audience collision, not a classroom-assignment miss. The same student account (`0374eb95-f83b-4a3b-9999-0ec6a32af1da`) is explicitly assigned `role_names = ['admin']` in `auth.users` / `user_roles` while also having a real student profile and active enrollment. Because `GET /exams` and `GET /exams/:id` are shared by student and staff surfaces, the backend inferred an admin/staff audience for this account and intersected student visibility with staff/private-exam visibility, which hid the private classroom exam from the student classroom page while still returning older public/past exams.
- [x] Patched the shared exam read contract to support an explicit additive `viewer=student` override for student surfaces. `packages/shared/src/schema/exams/exam-schema.ts`, `packages/services/src/api/exams/core.ts`, `packages/services/src/api/exams/types.ts`, and `packages/hooks/src/query/exams/use-exam-query.ts` now carry that additive audience hint. `app/sentinel-api/src/modules/examination/exams/controllers/get-exams.controller.ts` and `get-exam.controller.ts` now strip staff visibility scope when `viewer=student` is requested by a real student profile, and the student classroom/list/history/detail hooks now send `viewer: 'student'` so the student app always resolves classroom assignments as a student even when the same account also has elevated database roles.
- [x] Ran focused regression coverage for the new audience override using `pnpm --dir app/sentinel-api exec vitest run src/modules/examination/exams/controllers/get-exams.controller.test.ts src/modules/examination/exams/controllers/get-exam.controller.test.ts` and `pnpm --dir app/sentinel-web exec vitest run 'src/app/(protected)/student/exam/_hooks/use-exam-list.test.ts' 'src/app/(protected)/student/exam/[id]/_hooks/use-student-exam-data.test.tsx' 'src/app/(protected)/student/history/_hooks/use-student-history/index.test.ts' 'src/app/(protected)/student/history/details/_hooks/use-exam-details/index.test.tsx'`; all 28 focused tests passed on 2026-07-11.
- [ ] Ran `pnpm format:check` and `pnpm lint`; both surfaced unrelated repository-level blockers, so this plan is not marked release-complete from automation alone. `pnpm format:check` failed with existing Prettier drift across 109 files spanning API, web, core, support, shared docs, and unrelated exam/security modules. `pnpm lint` failed immediately in `packages/db` because the workspace lint script shells out to `eslint`, but the command is unavailable in that package environment (`sh: eslint: command not found`).

**Migration required:** No — validation only.

**Breaking changes:** None in this validation pass. Manual/live verification items remain the gating evidence for release readiness.
