# Issue and Implement

1. After the student submit the exam, it should redirect to a page that will show the total scores of the students for e.g [30/60] and also has a footer button for [turn_in] this will mark the exam as [turn_in / submitted]

2. The [alt + tab] is still working can you double check it and disable it similar to [ctrl + c / cmd + c]

3. Fix the dialog on the instructor on create-exam and update-exam dialog. The exam can be assign to multiple sections not just 1 section in which is the current implemented on the dialog - It should fetch all the section that was [assigned_subject] you can think of a better layout of it and how it will handle those changes. I suggest creating a similar dialogs on the offered subjects in which uses checkboxes.

4. Finalize now the grading page on instructor page wherein it handles the scores of the students that takes the exam. In addition it should be dynamic wherein if the exam is assigned to multiple sections with different intructors (that will be treated as proctors) the scores / grades are categorized and filtered by section

5. I need you to provide a [.md] file for this on @docs/to-do and ensure that you deeply analyze this pain and provide a to-do workflow and follow 1-3-1 rule and make sure to separate the task into phases in that way we can iterate or make some changes before proceeding to other phases
