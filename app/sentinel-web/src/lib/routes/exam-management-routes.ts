export function buildInstructorExamLogsHref(examId: string) {
    return `/exams/${examId}/logs`;
}

export function buildInstructorExamAssignHref(examId: string) {
    return `/exams/${examId}/assign`;
}
