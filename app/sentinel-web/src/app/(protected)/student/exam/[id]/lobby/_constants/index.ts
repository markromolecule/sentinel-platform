import {
    COMMON_HIGHLIGHT_ICONS,
    LOBBY_READINESS_ITEMS,
} from '@/app/(protected)/(instructor)/exams/[id]/preview/[sessionId]/_constants/preview-constants';

export const LOBBY_READINESS_CONFIG = {
    items: LOBBY_READINESS_ITEMS,
    icons: COMMON_HIGHLIGHT_ICONS,
};

export const LOBBY_TIMER_REFRESH_INTERVAL = 1000;
