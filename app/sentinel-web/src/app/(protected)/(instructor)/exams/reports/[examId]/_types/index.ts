/**
 * Type representing the categories of the instructor action queue.
 */
export type ActionQueueType = 'review' | 'makeup' | 'retake';

/**
 * Type representing the different tabs/sections of the detailed exam report page.
 */
export type ExamReportSection = 'overview' | 'attempts' | 'queue' | 'logs';
