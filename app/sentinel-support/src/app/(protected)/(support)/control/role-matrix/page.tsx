'use client';

import { AccessControlGovernanceForm } from '../_components/governance/control-governance-form';

/**
 * AccessControlRoleMatrixPage mounts the dynamic and filterable role matrix view.
 * Integrates into the unified horizontal sub-navigation layout.
 */
export default function AccessControlRoleMatrixPage() {
    return <AccessControlGovernanceForm />;
}
