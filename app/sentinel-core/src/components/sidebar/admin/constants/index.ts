import { ADMIN_NAVIGATION_SECTIONS } from '../../common/core-admin-nav-config';

export const DASHBOARD_ITEMS = ADMIN_NAVIGATION_SECTIONS[0]?.items ?? [];

export const MANAGEMENT_ITEMS = ADMIN_NAVIGATION_SECTIONS[1]?.items ?? [];

export const ANALYTICS_ITEMS = ADMIN_NAVIGATION_SECTIONS[2]?.items ?? [];

export const COMMUNICATION_ITEMS = ADMIN_NAVIGATION_SECTIONS[3]?.items ?? [];
