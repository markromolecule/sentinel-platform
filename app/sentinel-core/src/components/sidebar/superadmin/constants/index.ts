import { SUPERADMIN_NAVIGATION_SECTIONS } from '../../common/core-admin-nav-config';

export const DASHBOARD_ITEMS = SUPERADMIN_NAVIGATION_SECTIONS[0]?.items ?? [];

export const MANAGEMENT_ITEMS = SUPERADMIN_NAVIGATION_SECTIONS[1]?.items ?? [];

export const ANALYTICS_ITEMS = SUPERADMIN_NAVIGATION_SECTIONS[2]?.items ?? [];

export const COMMUNICATION_ITEMS = SUPERADMIN_NAVIGATION_SECTIONS[3]?.items ?? [];
